/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific Next.js configurations here if needed.
  // For example, to allow images from certain domains:
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
